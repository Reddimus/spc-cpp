foreach(example parse_outlook static_feed arcgis fire_weather archive watches)
    add_executable(example_${example} ${example}.cpp)
    target_link_libraries(example_${example} PRIVATE spc::spc)
    spc_set_warnings(example_${example})
endforeach()
