# Keeps the SHA-pinned GitHub Actions current. C++ dependencies are pinned in
# CMakeLists.txt and updated by hand.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
    commit-message:
      prefix: ci
